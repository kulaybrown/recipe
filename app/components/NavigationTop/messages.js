/*
 * NavigationTop Messages
 *
 * This contains all the text for the NavigationTop component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NavigationTop';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NavigationTop component!',
  },
});
